//Checking if a value (bool) is classified as a boolean primitive, simply if it's true or false. Return true or false.

function booWho(bool) {
 return bool === true || bool === false ? true : false;
}

booWho(null);
